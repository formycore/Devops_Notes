node {
    stage ('Docker check'){
        sh 'docker ps'
        sh 'echo ***************************'
        sh 'docker ps -a'
        sh 'echo ***************************'
        sh 'docker images'
    }
}