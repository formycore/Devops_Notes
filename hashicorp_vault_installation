'''
  - install the hashicorp vault with docker 
  - docker run -d --name vault -p 8200:8200 --cap-add=IPC_LOCK -e 'VAULT_DEV_ROOT_TOKEN_ID=root' -e 'VAULT_DEV_LIST_ADDRESS=0.0.0.0:8200' hashicorp/vault
  - start the container
'''
